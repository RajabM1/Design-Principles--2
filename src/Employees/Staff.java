package Employees;
public class Staff extends Employee {
    public Staff(int id, String name, int age, int hourlyRate, int completedHours) {
        super(id, name, age, hourlyRate, completedHours);
    }
}
